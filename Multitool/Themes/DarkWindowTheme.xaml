<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="Window" x:Key="DarkWindow">
        <Setter Property="Height" Value="{Binding Path=Data.Height, Mode=TwoWay}"/>
        <Setter Property="Width" Value="{Binding Path=Data.Width, Mode=TwoWay}"/>
        <Setter Property="Left" Value="{Binding Path=Data.Left, Mode=TwoWay}"/>
        <Setter Property="Top" Value="{Binding Path=Data.Top, Mode=TwoWay}"/>
        <Setter Property="ResizeMode" Value="NoResize"/>
        <Setter Property="WindowStyle" Value="None"/>
        <Setter Property="Background" Value="{StaticResource LightBlack}"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="{StaticResource DarkBlack}"/>
        <Setter Property="BorderThickness" Value="0.4"/>
    </Style>


</ResourceDictionary>